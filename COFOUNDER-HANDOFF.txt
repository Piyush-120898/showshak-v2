═══════════════════════════════════════════════════════════════
  SHOWSHAK — COFOUNDER HANDOFF & CONTINUATION BRIEF
  Purpose: paste the "KICKOFF PROMPT" (bottom of this file) at the
  start of a NEW chat so your AI cofounder resumes at full context
  and full intensity. The middle sections are the knowledge base.
  Keep this file updated as we progress.
═══════════════════════════════════════════════════════════════


───────────────────────────────────────────────────────────────
  0. HOW TO USE THIS FILE
───────────────────────────────────────────────────────────────
  1. Open a new chat.
  2. Copy the entire "KICKOFF PROMPT" block at the very bottom.
  3. Also tell it to read these files in the repo for full context:
       - overview                (the master project brief)
       - backend-architecture.md + backend-schema.md (the DB/stack)
       - points to keep in mind pitch wise.txt (strategy/revenue)
       - showshak-shared.js      (the heart of the app)
       - .kiro/specs/  (stack-sharing + stack-folder-view = SHIPPED, for reference;
         this is where future specs live too)
       - this file (COFOUNDER-HANDOFF.txt)
  4. Let it confirm understanding, then continue from "NEXT STEPS".


───────────────────────────────────────────────────────────────
  1. WHAT SHOWSHAK IS (one breath)
───────────────────────────────────────────────────────────────
  A cross-platform STREAMING DISCOVERY layer — NOT a streaming
  service, NOT social media. Curators post short vertical CLIPS
  recommending a show/movie; users feel something, trust the
  curator, tap "WATCH IT" → go to the streaming platform.

  Category we own: "Trust-based, creator-curated, action-oriented
  discovery." = Letterboxd's soul + TikTok's format + an action layer.

  The core loop:
    Curator posts a clip → user watches & trusts → taps Watch It
    → lands on Netflix/Prime/etc.

  We are building the WEBSITE first as the reference for the future
  NATIVE APP (founder knows web, not mobile yet).


───────────────────────────────────────────────────────────────
  2. SACRED PRODUCT PHILOSOPHY (never violate)
───────────────────────────────────────────────────────────────
  - CREATOR FIRST, SHOW SECOND. The clip never shows the title
    upfront — the title is revealed ONLY in the Watch It sheet.
    (If they've seen it, they skip; trust the curator first.)
  - FIRE (🔥) = the like. Not a heart. "This is lit."
  - WATCH IT = the climax. Never "Watch Now". Must feel
    frictionless and PRIVATE — never make the user feel their
    Watch It taps are tracked publicly.
  - HIDE THE SCOREBOARD: public surfaces show ONLY content signals
    (per-clip fires, now per-clip VIEWS) + creator + followers/clips.
    NEVER show fires-received total or Watch-It taps publicly —
    those are the curator's PRIVATE cockpit only, enforced in the DB
    by Row-Level Security (not just UI).
  - CURATORS, not influencers. Taste authorities.
  - NO open hashtags. Structured signals only (show pick + fixed
    vibe set + caption mining on our side).
  - Familiar interactions, ShowShak soul — never "just another
    TikTok/Netflix".


───────────────────────────────────────────────────────────────
  3. TECH STACK (the reality)
───────────────────────────────────────────────────────────────
  FRONTEND: pure HTML + CSS + vanilla JS. NO frameworks, NO build
    step, NO bundler. Runs via Live Server / GitHub Pages.
    - showshak-shared.js = the spine (the Clip_Engine, all ss* utils,
      auth/guest gate, surfaces, pooling, save/follow, events).
    - Pages: index (landing+onboarding), showshak-feed/discover/
      watchlist/profile/settings/upload.
    - Pure logic in showshak-shared.js is dual-exported (window.* +
      module.exports) for Node + fast-check PROPERTY TESTS.
      Run tests: `node tests/run-all.js`  (29+ files, fast-check).

  BACKEND: Supabase (Postgres + Auth + Storage + Edge Functions,
    Mumbai region). Migrations live in supabase/migrations/ and are
    APPLIED MANUALLY by the founder in the Supabase SQL editor.
    - Mux for video (HLS via <mux-player>, public playback IDs).
      NOW ON PAY-AS-YOU-GO (upgraded this session) — unlimited
      storage + $20/mo usage credit.
    - TMDB for titles + watch-providers. Now fetched SERVER-SIDE by the
      tmdb-providers Edge Function (search/link/manual/batch) — TMDB is
      DNS-blocked on Indian ISPs but reachable from Supabase's servers, so the
      old local data/ingest-tmdb.js VPN run is no longer required. Browser
      NEVER touches TMDB (it reads cached titles.providers, or proxies search
      through the function). The TMDB key is a function secret.

  GOLDEN BACKEND RULES:
    - Counts are DERIVED from event rows; *_count columns are caches.
    - Media → Mux/Storage; DB stores only IDs/URLs, never bytes.
    - Privacy enforced by RLS, not UI.
    - Soft deletes (deleted_at) everywhere; created_at everywhere.

  HOSTING: GitHub Pages (repo: github.com/Piyush-120898/showshak-v2,
    branch main). Pushing to main auto-rebuilds the live page.
    NOTE: Pages serves the FRONTEND only. SQL migrations + Edge
    Function deploys are SEPARATE founder steps in Supabase.


───────────────────────────────────────────────────────────────
  4. WHAT'S ALREADY BUILT (code-complete, verified)
───────────────────────────────────────────────────────────────
  Frontend prototype: DONE (all pages).
  Backend, spec-by-spec (all CODE done; some founder-run ops open):
    - Auth / guest-first onboarding ✓
    - curator-upload-v2 ✓ (real publishing pipeline, trim, titles,
      drafts, edit-after-post, multi-title Watch It)
    - watch-it-availability ✓ (cache-first, region-aware; TMDB ingest)
    - mux-video-clips ✓ (Mux pipeline, Edge Functions, preloader)
    - unified-clip-player ✓ (one Clip_Engine for feed + viewer)
    - creator-analytics ✓ (real cockpit; RLS-gated; migration 0019)
    - public-curator-profile ✓ (real curator by username)
    - curator-role-persistence ✓ (migration 0020 trigger)
    - clip-player-performance ✓ (Phases 1-5; A-grade player, 6 props green)
    - PWA layer ✓ (installable manifest+icon, smart SW caching, iOS
      safe-area, launch splash) — the "later PWA layer" is now started
    - TMDB server-side ingest ✓ (tmdb-providers edge function: search/
      link/manual/batch — no local VPN script needed)
    - Live TMDB title search in upload + instant link + manual-with-platform ✓
    - Clip + profile share deep-links (title-blind) ✓; Watch It opens platform ✓
    - Views synced (card == analytics) ✓; migrations 0001–0021 applied ✓
    - stack-sharing ✓ (Phase 1+2: private/unlisted/public + collaborative; migrations 0022/0023)
    - stack-folder-view ✓ (folder route + share chooser + Watchlist preview + Feature-in-Highlights)
    - settings overhaul ✓ (account/creator DB-wired; live handle uniqueness; deactivate/delete + 30-day retention)
    - views self-collapse ✓ (0024 — curator self-views count once); feed unmute carries on scroll ✓
    - SUITE: 89 files green; migrations 0001–0031 applied (0030 reserved for DMCA Phase 2)
    - feed-clip-load-performance ✓ (A-grade feed clip-load + caching pipeline;
      9 prop-feed-* properties; the heavy prefetch/SW-cache pieces are OPT-IN —
      see SESSION LOG; the iOS feed-stall root cause = unbounded player mount, fixed)
    - dmca-moderation-scaffolding Phase 1 ✓ (attestation + intake + policy surfaces +
      audit log; migration 0029 applied; submit-takedown fn code pushed, NOT yet deployed)
    - beta-consent-gate ✓ (DPDP consent + 18+ onboarding gate; one-time Curator Terms
      acceptance at Become-a-Curator; migration 0031 applied; legal-text seed READY,
      founder to run — see §7)
    - feed-follows ✓ (tiered trust-weighted feed ranker behind ssLoadClips: followed-new →
      other-new → followed-popular → global-popular → randomized rest; pure ssRankFeed +
      ssSliceRankedPage + ssFeedTier + ssPopularityScore, 13 fast-check properties + 3
      unit-feed-* shell tests; client-side localStorage seen-state — NO migration;
      ss_ff_ranker kill switch + automatic flat-feed fallback; SHIPPED to main, commit
      05afc86, CACHE_VERSION v31)
    - felt-quality follow-ups ✓ (clip vignette lightened — clip now reads as the foreground,
      not a dimmed backdrop; profile follower/clip-count stats prefetched into the prewarm
      cache so they paint INSTANTLY on landing; CACHE_VERSION v32 — see SESSION LOG)


───────────────────────────────────────────────────────────────
  5. SESSION LOG (most recent first; all pushed to main)
───────────────────────────────────────────────────────────────
  ── MOST RECENT (feed-follows SHIPPED + felt-quality follow-ups; suite 73 → 89 files green) ──
    CONTEXT: this session built and SHIPPED feed-follows — the cold-start lever that
    finally wires the follow graph into feed ordering — as a full spec
    (.kiro/specs/feed-follows/, requirements → design → tasks, executed task-by-task,
    phased so the Feed never breaks). Then two felt-quality follow-ups (clip brightness
    + instant profile stats). Suite 73 → 89 files green (+13 prop-feed-* ranker
    properties, +3 unit-feed-* shell tests). NO migration this session.

    • feed-follows — the flat newest-first feed is now a TIERED, TRUST-WEIGHTED feed:
        1) new clips from curators you follow → 2) other new clips → 3) followed
        curators' older popular clips → 4) global popular → 5) randomized rest.
        - PURE CORE (dual-exported, fast-check, 13 properties): ssRankFeed (the tiered
          ranker), ssSliceRankedPage (stable pagination), ssFeedTier (Recency_Window =
          14 days), ssPopularityScore (fires*3 + views*1 — public signals only), plus a
          seeded PRNG (_ssXmur3/_ssMulberry32/_ssSeededShuffle) so Tier 5 is deterministic
          and pages never reshuffle. Exactly-once de-dup (highest tier wins), seen clips
          sink WITHIN their tier, empty-follow/guest degrades to ~today's feed.
        - HIDE THE SCOREBOARD enforced at the data layer: the candidate query selects
          ONLY id, creator_id, created_at, fires_count, views_count — never a private
          metric (watch-it taps / fires-received / analytics). Ranking can't leak the
          scoreboard. (unit-feed-projection.test.js asserts this structurally.)
        - IMPURE SHELL: ssLoadClips now orchestrates candidate fetch → ssRankFeed (cached
          once per feed session) → ssSliceRankedPage → hydrate the page via the existing
          rich select → ssMapContentRowsToClips. Signature + clip shape UNCHANGED.
          Automatic flat-feed fallback on ANY error (Req 8) + an on-device kill switch
          localStorage ss_ff_ranker='off'. _ssEnsureFeedSession caches the ranked list
          (stable pagination); _ssFollowedCreatorIds reads real follow rows;
          _ssReadSeenState reads localStorage 'ss_seen_v1_<uid>'.
        - SEEN-STATE = NO MIGRATION: written client-side next to ssRecordView
          (_ssMarkSeen → localStorage 'ss_seen_v1_<uid>', capped 500, guests skipped),
          because view_events has no client SELECT grant (0019). A server-side seen
          policy is an OPTIONAL future nicety; the feature is correct without it.
        - BUG CAUGHT BY THE TESTS (would have shipped a BLANK FEED): the candidate query
          selects no `status` column (scoreboard-safe), but ssRankFeed's eligibility
          filter rejected rows with absent status → ranker returned [] whenever clips
          existed; only the kill switch worked. Fixed: absent status/deleted_at = eligible
          (the SQL already guarantees liveness), present must be exactly 'live'/null;
          reconciled Property 11 + requirements (the latent Req 3.3 vs 9.4 conflict).
        - SHIPPED: pushed to main, commit 05afc86, CACHE_VERSION v31. Frontend-only —
          no migration, no Edge Function. The follow loop is now live in code; the feed
          rewards trust the moment curators have followers.

    • felt-quality follow-ups (on-device feedback): CACHE_VERSION v31 → v32.
        - CLIP BRIGHTNESS: the playing clip felt "dull / in the background." Root cause =
          the .clip-vignette (and the fullscreen .ssv-vig) scrim: 0.92 black at the bottom
          PLUS a dark tint across the whole TOP. Lightened both — bottom legibility
          gradient kept (0.80, clears by ~46–50%) but the TOP tint removed → the clip now
          reads as the foreground. Did NOT add a brightness() filter on the video (would
          blow out highlights / over-process); the scrim was the real cause.
        - INSTANT PROFILE STATS: followers/clips counts lagged on landing because
          ssPrewarmProfile cached identity but NOT the counts (they waited on async
          queries). Now the prewarm ALSO fetches the follower count + clip count (cheap
          head-only count queries) into the profile cache; applyCachedOwnProfile seeds
          PROFILE.followers + a cached clip count so the creds strip paints REAL numbers
          on the first frame, then revalidates. ssWriteMyProfileCache made merge-safe so
          identity ↔ counts never clobber each other; fetchOwnFollowers +
          hydrateMyClipsFromDB refresh the cached counts so repeat visits stay accurate.
        - ⚠ v32 (clip brightness + instant profile stats) is CODE-COMPLETE + suite green
          but PUSH-PENDING at the time of writing — confirm it's on main.

  ── PREVIOUS (beta-consent-gate SHIPPED + DMCA Phase 1 SHIPPED & APPLIED; pushed to main, commit 99e004a, CACHE_VERSION v30; suite 73 files green) ──
    CONTEXT: this session SHIPPED the beta-consent-gate feature (full spec
    .kiro/specs/beta-consent-gate/, requirements → design → tasks, executed task-by-
    task) and PUSHED both it and the previously-built DMCA Phase 1 to main. Founder
    applied migrations 0029 + 0031 and enabled Anonymous sign-ins. Suite 72 → 73
    files green (+1 prop-consent with 7 properties). The DMCA-Phase-1 entry below is
    now LIVE (was "not yet pushed").

    • beta-consent-gate — the DPDP open-beta compliance unit (one lean phase):
        - CONSENT + 18+ GATE as onboarding Step 1 in index.html: two UNTICKED-by-
          default checkboxes (affirmative consent + 18+), ToS/Privacy links bound to
          the exact version being recorded, advance disabled until both checked AND
          policies resolve, records consent via ssRecordConsent BEFORE any personal-
          data collection (DPDP affirmative-consent, no pre-ticked boxes).
        - CURATOR TERMS acceptance at "Become a Curator" (showshak-profile.html /
          bcActivate): the agree checkbox is now version-stamped + recorded to the DB
          (ssRecordCuratorTerms) BEFORE the users.role flip; flip is blocked if the
          acceptance can't be saved. This is a ONE-TIME relationship-level consent
          (founder confirmed once is enough) — DISTINCT from the per-clip attestation,
          which stays per-upload as the safe-harbor proof.
        - LICENSE FRAMING CORRECTED (cofounder pushback): curator KEEPS ownership and
          GRANTS ShowShak a non-exclusive/worldwide/royalty-free/sublicensable license
          to host/transcode/show — NOT "ShowShak has no rights" (which would make it
          impossible to run the product). legal/curator-terms.md authored to this.
        - Migration 0031_beta_consent_gate.sql (APPLIED ✓): consents table with a
          `kind` discriminator ('user_consent'|'curator_terms') + nullable per-kind
          version cols + per-kind CHECK constraints + own-row RLS; ss_record_consent
          + ss_record_curator_terms SECURITY DEFINER RPCs; AND it WIDENS the 0029
          policy_versions.doc CHECK to allow 'curator' (caught mid-build — the seed
          would've failed otherwise).
        - Pure (dual-exported, fast-check): ssConsentComplete, ssCuratorTermsAccepted,
          ssPolicyNeedsCounselReview (tests/prop-consent.test.js, 7 properties).
          Wrappers (window-only, fail-soft, never-throw): ssRecordConsent,
          ssRecordCuratorTerms, ssCurrentPolicyVersions (mints an anonymous session
          lazily so guest consent has a real auth.uid() under RLS).
        - showshak-legal.html now renders the CURRENT seeded policy version (incl. a
          new doc='curator' tab), honors ?v=<version>, keeps the counsel banner while
          [..] tokens remain, and degrades to placeholder scaffolding when unseeded.
        - sw.js CACHE_VERSION v29 → v30.
        - supabase/seed/seed_policy_versions.sql: READY-TO-RUN, real legal/*.md bodies
          inlined verbatim ($SS$ dollar-quoted), idempotent. ⚠️ FOUNDER STILL TO RUN
          IT (see §7) — until then the legal page shows placeholder + the gates
          fail-soft.

    • IDENTITY/AUTH note: the consent gate establishes the guest-first anonymous-
      session model the backend doc always envisioned — anon session minted only at
      the advance action (not on render), carries no PII, links to the permanent
      account on later sign-in.

  ── PREVIOUS (DMCA/MODERATION spec + Phase 1 — now SHIPPED & APPLIED this session; suite 72→73 files green) ──
    CONTEXT: this session scoped the DMCA / moderation feature as a full spec
    (.kiro/specs/dmca-moderation-scaffolding/ — requirements → design → tasks, all
    validated) and BUILT Phase 1 of it. Suite is now 72 property-test files green
    (was 69; +3 new prop-dmca-*). NOTHING from this session is pushed to main yet,
    and migration 0029 is NOT yet applied — see §7.

    THE POSTURE WE LOCKED (founder + cofounder): launch an OPEN BETA in India to
    prove the loop (~15-20 curators, 300-500 clips, ~1000-1500 users), then raise
    on that traction and buy a real legal team for the heavyweight compliance. The
    site is PUBLIC (anyone with the link); we control risk by LIMITING the curator
    (upload) side, not by a "private"/"just testing" label (a disclaimer is NOT
    legal protection — corrected the founder on this). Phase 1 IS the beta-safe
    posture; ship exactly it, not less. Music feature stays PARKED (a copyrighted-
    song library = inducement). 18+ only (DPDP children rule).

    • DMCA/MODERATION — PHASE 1 BUILT (Posture & Intake). All code-complete, suite
      green, but UNPUSHED + migration UNAPPLIED:
        - 3 pure fns + property tests in showshak-shared.js (dual-exported):
          ssAttestationComplete (Req1), ssDmcaNoticeWellFormed (Req3),
          ssContentPubliclyVisible (Req4). Tests: prop-dmca-attestation/notice/
          visibility.test.js.
        - Migration 0029_dmca_intake_posture.sql (FOUNDER MUST APPLY): tables
          attestations / policy_versions / complaints (closed 7-state) /
          moderation_log (append-only); the moderation_log immutability triggers
          (reject UPDATE/DELETE for ALL roles incl. service); the
          content_requires_attestation trigger (blocks any *→'live' with no
          attestation row); ss_is_admin(); and 3 SECURITY DEFINER RPCs
          (ss_submit_complaint [anon], ss_record_attestation [authed],
          ss_get_policy_version). Removal = RLS: the existing read_live_content
          policy already hides status='removed'/soft-deleted from public reads.
        - Edge Function supabase/functions/submit-takedown/ (Deno; FOUNDER MUST
          DEPLOY --no-verify-jwt) — public anon intake, re-validates the notice,
          calls ss_submit_complaint, returns only a confirmation_ref.
        - Client wrappers (window-only): ssRecordAttestation, ssSubmitTakedown,
          ssLoadPolicyVersion.
        - Surfaces: showshak-legal.html (4 policy docs tos/privacy/copyright/
          community, render from policy_versions w/ placeholder fallback);
          showshak-dmca.html (public takedown intake form, title-blind);
          attestation+indemnity gate injected into showshak-upload.html publish()
          (hard-blocks publish until accepted, then ssRecordAttestation — REQUIRES
          0029); wired the 4 dead legal links in showshak-settings.html.
        - sw.js: CACHE_VERSION v28 → v29; legal/dmca pages precached.

    • DMCA/MODERATION — PHASE 2 NOT BUILT (Workflow & Compliance; tasks 11-18 in
      tasks.md): pure fns ssComplaintTransition (+SS_COMPLAINT_INVALID),
      ssReinstatementDue, ssRepeatInfringerDecision, ssAckClockState + tests;
      migration 0030 (termination markers) + RPCs ss_moderate_complaint /
      ss_file_counter_notice / ss_terminate_curator; showshak-moderation.html
      (RLS-gated admin queue); showshak-counter-notice.html; showshak-dmca-rls-
      check.html. DECISION: DEFER Phase 2 to post-raise / when we actually receive
      takedowns — at 500 clips the founder can action a takedown by flipping
      content.status='removed' in the Supabase editor (RLS hides it everywhere).

    • INDIA LEGAL DRAFTS written in legal/ (README + terms-of-service.md +
      privacy-policy.md + copyright-policy.md + community-guidelines.md). Real,
      India-correct working drafts (NOT placeholders), every doc marked "DRAFT —
      counsel review required". Grounded in: IT Act 2000 §79 + IT Rules 2021
      (Grievance Officer: acknowledge ≤24h, resolve ≤15 days; we are NOT an SSMI
      <5M users so the heavy CCO/Nodal-officer/monthly-report duties don't bind);
      Copyright Act 1957 + Copyright Rules 2013 Rule 75 (restrict ≤36h, 21-day
      court-order window, restore if none — India has NO "DMCA", that's US);
      DPDP Act 2023 + DPDP Rules 2025 (NOTIFIED 13 Nov 2025; phased — consent-
      manager reg ~Nov 2026, substantive notice/consent/rights/penalties ~May
      2027). Framing: OPEN beta, India-only, NON-COMMERCIAL (no pay/ads), neutral
      host + creator responsibility, 18+. The .md files are the SOURCE/counsel copy
      — the user-facing render is showshak-legal.html (which still shows its own
      placeholder text until we sync the real text in / seed policy_versions).
      [PLACEHOLDERS] in the docs (entity name/type, address, city, grievance +
      copyright + support emails, officer name, effective date, version, URL) MUST
      be filled by the founder/counsel.

    • PROPOSED, NOT YET BUILT (agreed next): (1) sync the real policy text into
      showshak-legal.html so the beta shows it; (2) add a CONSENT + 18+ GATE as
      onboarding Step 1 — an UNTICKED-by-default mandatory checkbox with clickable
      ToS/Privacy links, version-stamped into the DB (who/when/which versions),
      mirroring the attestation pattern. DPDP: consent must be free/specific/
      informed/unambiguous + affirmative (no pre-ticked boxes); record it.

    • OPEN QUESTION FOR FOUNDER: the operating entity — registered company vs
      individual/sole proprietor for the beta — needed to fill the legal docs'
      entity/liability/grievance sections.

  ── PREVIOUS (felt-quality + reliability sweep v21→v28; trim moved to Mux; profile/discover/settings polish; all pushed to main; last commit 5185c9d) ──
    CONTEXT: suite now 69 property-test files green. A run of on-device bug fixes
    and felt-quality passes shipped across CACHE_VERSION v21→v25. The headline:
    client-side ffmpeg.wasm trim is GONE — trimming is now SERVER-SIDE on Mux.

    • ANDROID "NaN"/"undefined" — FIXED (v21). The earlier OPEN BUG: fmtFires(n)
      now coerces non-finite → 0; _inlineClipHTML + _inlineSyncRail default
      fires/platLabel/platColor/platRgb/platAbbr so a missing field never prints
      "NaN"/"undefined" on the Fire pill or Watch It label. Root = a stale Android
      SW bundle + the un-defaulted inline render path; both addressed (defense-in-depth).

    • iOS INLINE FEED STALL — tap-to-play affordance (v21). iOS/WebKit caps live
      <video>s, so a contended active inline clip can stick on the first frame.
      New pure ssShouldShowTapToPlay({active,isVideo,playing}) + a VideoSurface
      play-state hook (playing/pause/waiting/stalled + timeupdate) show the centre
      pause/play glyph when the active clip isn't actually playing; a tap already
      opens the bounded fullscreen viewer (which always plays). Property-tested
      (tests/prop-tap-to-play.test.js).

    • CURATOR AVATAR PHOTO + felt-quality (v21/v22). The clip bottom + viewer now
      render the curator's avatar_url PHOTO (object-fit cover, letter fallback);
      _ssvNormalize carries avatar; _ssAvatarInner() is the shared renderer. Avatar
      + handle sizes bumped (feed 32px/13px, viewer 36px/15px). Overlay info + rails
      dimmed (avatar stays opaque); the Watch It breathing pulse replaced by a
      subtle fire-orange ssTraceBorder; Watch It made more translucent (opacity 0.78).

    • PROFILE COLD-START PREWARM (v22). ssPrewarmProfile() (idle + on session
      resolve) caches the signed-in user's identity row + warms the avatar image +
      prefetches the profile document; the Profile page paints from
      ssReadMyProfileCache() then revalidates (applyCachedOwnProfile in boot()).
      Cache cleared on sign-out. Feed→Profile is instant on repeat visits.

    • SERVER-SIDE MUX TRIM (v23) — replaces the flaky 25 MB ffmpeg.wasm browser
      trim (the "Preparing editor…" stall + "couldn't trim — posting full clip"
      fallback). Now: upload the FULL source + meta.trim={in,out}; the mux-webhook
      clips the source asset in TWO PHASES — (1) source ready → create a clip asset
      via input mux://assets/<id> start_time/end_time, store mux_clip_asset_id +
      mux_source_asset_id in meta, stay 'processing'; (2) clip ready (matched by
      meta->>mux_clip_asset_id) → flip live with the CLIP + DELETE the source so
      ONLY the trimmed part is stored. Never-stuck fallback: clip-create failure
      publishes the full source. Plus a client-side TRIM PREVIEW (play/loop the
      exact cut locally on the Trim step — no upload, no ffmpeg). ⚠️ FOUNDER
      DEPLOYED mux-webhook (`supabase functions deploy mux-webhook --no-verify-jwt`)
      — CONFIRMED WORKING on device.

    • RESOLUTION 480p→720p AUTO-CLIMB (v24). SS_RES_CAP is now 720p on ALL tiers
      (was slow:480p hard cap → clips were stuck at 480p). The low initial-bandwidth
      seed keeps a fast ~480p start; ABR climbs to 720p when bandwidth is free
      (ceiling, not target — stays low on a genuinely slow link). prop-network-policy
      updated. (Mux "basic" assets top out at 720p anyway.)

    • UPLOAD AVAILABILITY RACE (v24). upPickTmdb links via the edge `link` mode
      which caches TMDB providers in the BACKGROUND, so the Confirm_Availability
      step wrongly said "couldn't confirm where this streams." upRefreshTitleProviders()
      now polls the title row (4×, 2s apart) and re-renders so upload-time
      availability matches the live Watch It.

    • PUBLIC-PROFILE CLIP VIEWS = 0 — FIXED (v25). hydrateCuratorProfile's content
      select was MISSING views_count → every public clip mapped to 0 views. Added
      it; the public card now matches owner/feed counts (ssMapContentRowsToClips →
      ssDisplayViews).

    • FOLLOWING list/count CONSISTENCY — FIXED (v25). Root: there was NO
      ssHydrateFollowing() (stacks had ssHydrateStacks, following didn't), so the
      local store (sessionStorage 'ss_following_v1') was empty on a fresh session →
      the count/buttons read 0 while renderFollowing did its OWN DB fetch → mismatch
      ("count says 0 but a profile shows under Following") + cross-session resets.
      Added ssHydrateFollowing() (seeds the local store from DB follows on
      session-resolve + fresh login, REPLACING stale/mock-only follows), made
      renderFollowing render from that single DB-seeded store (no per-render DB
      fetch → no unfollow flicker/phantom), and subscribed the profile to
      ssOnFollowingChange to re-render list+count+tab live.

    • PORTRAIT LOCK (v26). ShowShak is portrait-only. manifest orientation
      "portrait" locks the INSTALLED PWA on Android; iOS ignores it, so shared.js
      injects a rotate-to-portrait overlay (pure CSS media query, touch-phones-
      only via landscape + short-height + coarse-pointer). index.html now ALSO
      loads showshak-shared.js so the lock + shared utils cover the landing too
      (DB/auth features there guarded → no-op).

    • ANALYTICS "TOP PERFORMING CLIPS" FIXED (v27). Three causes: (1) rows drew
      only the gradient → now render the clip poster; (2) watch-its were FABRICATED
      (fires×0.18) + fires came from fires_count → now use REAL PROFILE.perClip
      (0019 reader), with views shown too, ranked by performance; (3) fetchOwnAnalytics
      populated PROFILE.perClip but only called renderPrimary() → added
      renderAnalytics() so the list refreshes when per-clip data lands (this was
      why the numbers didn't reconcile with the totals).

    • HIGHLIGHTS reworked (v27). The old ★ feature-toggle sat at the SAME
      coordinates as the clip count (top:9px left:9px) → taps hit the count and
      bubbled to the card (the "unclickable star"). Removed the star. The owner
      Highlights shelf now shows ONLY creator-selected (highlighted) stacks (not
      all), with a "+ Add" picker in the header (lists all stacks, Feature/Featured
      toggle; featuring makes the stack public) and a reliable ✕ remove on cards.

    • DISCOVER + SETTINGS platform cleanup (v28). Discover chips trimmed to the
      major India set (Netflix · Prime Video · JioHotstar · SonyLIV · Zee5 · Apple
      TV+) — dropped Disney+ (duplicated JioHotstar) + HBO Max (not in India), fixed
      in data/showshak-data.js PLATFORM_FILTER_ORDER. Settings → My Platforms now
      split into MAJOR + REGIONAL sections (MAJOR_PLATFORMS set; toggle indices
      preserved); offline-fallback mock cleaned.

    • BRAND ASSET TOOL (committed): tools/brand-assets.html — a dependency-free
      canvas generator that exports PNGs from the real logo + brand fonts: app icon
      (square + rounded), Instagram "Coming Soon" splash (square + 9:16 story), and
      CLEAN pitch-deck title cards (16:9 + square, no Coming Soon). Open in a
      browser, click to download. Dev tool, not part of the app.

    • MUSIC / DMCA STRATEGY (decision, NOT built). Founder asked about letting
      curators add songs. Conclusion: (a) NO Spotify/Apple API — those give
      metadata + 30s previews only, NOT a sync/usage license. (b) The only safe
      API path is ROYALTY-FREE catalogs that bundle a license (Pixabay/Jamendo to
      start; Uppbeat/Epidemic later). (c) "Creator takes responsibility + we run
      DMCA takedown" IS the standard UGC posture and CAN work — BUT only while we
      stay a NEUTRAL HOST; building a "use this song" library of copyrighted music
      = inducement → loses safe harbor, and music is the most aggressively enforced
      (esp. Indian labels). So: clips = creator-responsibility + takedown; music =
      royalty-free only, deferred. CRUCIAL: real safe harbor needs the machinery —
      ToS + creator attestation/indemnity, a registered DMCA agent (US) + an
      India-resident Grievance Officer (IT Act 2021: 36-hour ack), expeditious
      removal, a repeat-infringer termination policy, logging. That IS the
      DMCA/moderation scaffolding below — it's a prerequisite, not polish. (Not
      legal advice; needs real IP counsel before launch.)

    • DEPLOY STATE: CACHE_VERSION = v28 (sw.js). All work pushed to main (last
      commit 5185c9d). mux-webhook REDEPLOYED for server-side trim (confirmed
      working on device). Suite 69 property-test files green. Migrations through
      0028 applied. After any future push the founder needs on-device, BUMP
      CACHE_VERSION + reopen the installed PWA twice.

  ── PREVIOUS (FEED CLIP-LOAD PERFORMANCE build + the iOS feed-stall saga; CACHE_VERSION was v20; commit bc2064c) ──
    CONTEXT: suite is now 68 property-test files green. watch-it-curator-
    availability is SHIPPED (migration 0028 applied by founder). The big work
    this stretch was the FEED CLIP-LOAD PERFORMANCE spec + chasing an iOS-only
    feed playback bug to its real root cause.

    • feed-clip-load-performance spec — BUILT & shipped (.kiro/specs/feed-clip-
      load-performance/, + shareable architecture doc feed-cache-pipeline.md at
      repo root). One principle: "the active clip always wins the pipe; everything
      else is prefetched only with spare, bounded bandwidth." Phases 0-4:
        - Pure decision core (dual-exported, property-tested, 9 prop-feed-*.test.js):
          ssPreloadTier (preload ladder), ssShouldDeepen (progressive deepening
          gate), ssSplashLift (cold-start splash lift truth table), ssSegmentEvictionPlan
          (LRU+window segment eviction). Plus ssNetworkPolicy carries {preloadDepth,
          maxResolution}. Resolution cap = 720p everywhere / 480p on slow (Option A).
        - Impure wiring: preload tiering on the mux-players (active=auto, next few=
          metadata, rest=none); a progressive-deepening controller; a session byte
          budget + circuit breaker; a CORS first-segment warm rewrite; a cold-start
          splash lane in showshak-feed.html; metadata window bumped to 30
          (SS_FEED_CACHE_MAX 10->30 = SS_METADATA_WINDOW); a persistent range-aware
          (HTTP 206) SW Segment_Cache in a SEPARATE 'showshak-seg' bucket in sw.js.
      ⚠️ IMPORTANT — most of that heavy pipeline is now OPT-IN / OFF BY DEFAULT
      (see below) because it caused regressions. What's LIVE & ON by default:
      preload tiering, the bounded player pool, poster warming, cold-start splash
      TIMING. What's OFF by default (opt-in via localStorage ss_ff_segcache='on'):
      the SW Segment_Cache AND all explicit first-segment prefetch (warm/deepen/
      cold-start), because without the SW cache those fetches just saturate the
      stream.mux.com connection pool and starve the active clip.

    • THE iOS FEED-STALL BUG — ROOT CAUSE FOUND & FIXED (commit bc2064c).
      Symptom: in the INLINE feed only (fullscreen was always fine), after the
      first couple of clips, a clip would clear its poster, show the first frame,
      then FREEZE; sometimes another clip played (muted) in the background. Only
      on iOS; Android played fine.
      The hunt (each was a real contributor, fixed in order):
        1. SW Segment_Cache 206 path + heavy prefetch saturating stream.mux.com
           -> made the SW cache + ALL explicit segment prefetch OPT-IN (off by
           default). Helpers: _ssFeatureOff(name) and _ssSegPrefetchOn() read
           localStorage flags (ss_ff_tiering/deepen/coldstart/segcache).
        2. play() now FORCES preload='auto' on the active clip (was racing the
           tier assignment, leaving it stuck after the poster).
        3. _ssMuteOthers now PAUSES + mutes every non-active surface (was only
           muting) so a recycled player can never keep playing in the background
           and steal a video slot (iOS allows very few simultaneous videos).
        4. ***THE REAL ROOT CAUSE*** (confirmed in code): mountInline did
           `_inlineClips.forEach(_inlineWireClip)` — it mounted a <mux-player>
           for EVERY clip (up to Metadata_Window=30 after the cache bump). The
           FULLSCREEN viewer (ssOpenClip) already mounts only a bounded band via
           _ssvPruneSurfaces — which is EXACTLY why fullscreen never had the bug.
           iOS/WebKit strictly limits simultaneously-loaded <video> elements, so
           the over-mounted feed left clips stuck on the first frame; Android is
           lenient so it looked fine. FIX: mountInline now mounts only
           ssMountedPlayerSet(0, n, SS_MAX_LIVE_PLAYERS); appendInline no longer
           eager-mounts appended clips. The IntersectionObserver + pruneInlineSurfaces
           mount the rest on demand, bounded to SS_MAX_LIVE_PLAYERS=4 — the exact
           band proven to work on iOS in the fullscreen viewer. NO platform fork.
      DECISION (founder + cofounder agreed): do NOT fork iOS vs Android behaviour.
      Build ONE behaviour correct for the strict platform (iOS) — it's leaner for
      Android too. Pool size could become a small per-platform tuning knob LATER
      if ever needed, but band=4 is proven on iOS so we did not change it.

    • ✓ FIXED in v21 (see most-recent entry) — was: Android shows "NaN" under the fire icon on
      some clips, and "undefined" on the Watch It button's platform label; the
      SAME clip shows "0" and a normal Watch It on iPhone. So it's a per-render
      data-default gap (c.fires undefined -> undefined+0 = NaN -> fmtFires prints
      "NaN"; platAbbr/platColor undefined -> "undefined"). Why Android-only is
      the key clue: most likely Android is running a STALE SW-cached older JS
      bundle (it had not picked up the newest CACHE_VERSION yet), OR a feed clip
      path isn't defaulting fields the way the viewer's _ssvNormalize does.
      RECOMMENDED FIX (next session, low-risk, bulletproof regardless of cause):
        - Harden fmtFires(n): coerce non-finite -> 0 (`if (!isFinite(n)) n = 0;`).
        - In _inlineClipHTML + _inlineSyncRail use `Number(c.fires)||0` and default
          platAbbr/platColor/platLabel/platRgb so a missing field never prints
          "undefined"/"NaN".
        - First confirm the Android device is actually on CACHE_VERSION v20
          (reopen twice / clear site data). If it clears after update, it was the
          stale bundle; still ship the display guards as defense-in-depth.

    • DEPLOY STATE: CACHE_VERSION = v20 (sw.js). All work pushed to main; last
      commit bc2064c. After any future push that the founder needs on-device,
      BUMP CACHE_VERSION and reopen the installed PWA twice.

  ── MOST RECENT (PWA perf + felt-quality + watch-it availability; CACHE_VERSION v14) ──
    • PWA-BLACK-SCREEN-LOAD spec — SHIPPED Phase 1 + 2 (live on main):
        - Phase 1 (the bug kill): removed the body{opacity:0} gate, added an
          always-present static shell on all 6 app pages, deferred the render-
          blocking scripts (supabase-js etc.) in-order with page-init on
          DOMContentLoaded, and made the body-reveal fire on internal navs (not
          just bfcache). First paint no longer waits on JS → the 5–6s black
          screen on feed↔page↔feed is GONE. Pure helpers ssResolveFirstFrame +
          ssShouldRevealBody (property-tested). Fixed the stale sw.js comment.
        - Phase 2 (delight): cross-document View Transitions (@view-transition +
          ~160ms cross-fade, reduced-motion guard, graceful instant-cut fallback);
          ssNavStrategy reconciles ssNavigate so exactly ONE animation runs.
        - Phase 3 (prerender + bfcache) is SPECCED but NOT built (deferred).
    • BRANDED SPLASH: first-ever launch = ~3s brand beat (logo+tagline) that
      doubles as a tier-aware warm-burst prefetch window (8/5/2 clips by network);
      every later open = quick ~700ms splash; NEVER on internal navigation.
      (showshak-feed.html splash block; localStorage 'ss_launched_v1'.)
    • FEED AUDIO BUG FIXED (was: see clip C, hear clip B on fast scroll): root
      cause = the .catch() in _activatePostUnlock/_ssActivateSurface blindly
      retried play() on a scrolled-away surface (resurrected its audio). Fix:
      gate every retry/unmute continuation on surface.intendsToPlay() (new
      wantPlay getter), PLUS a belt-and-braces _ssMuteOthers(surfaces, active)
      invariant on every setActive in BOTH hosts → only the visible clip is audible.
    • WATCHLIST recommended grid now loads REAL clips (cache-then-revalidate via
      ssReadPageCache 'watchlist'), not the old SSData mock.
    • STACK creator shown by display NAME (not @handle); normal users show their
      real name non-clickable, curators stay clickable; fixed the "@curator"
      fallback. Needed migration 0026 (get_shared_stack returns owner {name,
      username, is_curator} + member names). ssStackContributors carries name.
    • INDIA PLATFORM CATALOG refreshed + verified (deep web research): migration
      0027 deactivates Disney+ / HBO Max / Hulu (merged-into-JioHotstar or not in
      India — never deleted, FKs intact) and seeds the real set: Netflix, Prime
      Video, JioHotstar, SonyLIV, Zee5, Apple TV+, YouTube + regional (Aha, Sun
      NXT, Hoichoi, Chaupal, KableOne, STAGE, Planet Marathi, ManoramaMax, ETV
      Win) + Crunchyroll. ssPlatformWatchUrl: added YouTube/Crunchyroll routing,
      Apple TV+ → /in/, HBO Max → JioHotstar; regional app-first ones fall back to
      the Google "watch on" resolver. (Excluded on purpose: ALTBalaji/ALTT —
      govt-BANNED 2025; Ullu/adult; Tata Play/Airtel Xstream — aggregators.)
    • NEW ACTIVE SPEC — watch-it-curator-availability: requirements + design +
      tasks ALL written & validated; NOT yet implemented. THIS is the next build
      (see §6b + NEXT STEP). Migrations 0026 + 0027 are APPLIED in Supabase.

  ── RECENT (stacks + settings + fixes; suite 45 files green) ──
    • stack-sharing Phase 1+2 (private/unlisted/public + collaborative) —
      migrations 0022 / 0023; 6 pure rules property-tested.
    • stack-folder-view — deep-linkable folder route (showshak-stack.html?stack=),
      title-blind grid, tap→viewer-with-stack-playlist, Share visibility-chooser →
      native share, Watchlist preview cap 12 + "View All", Feature-in-Highlights;
      shared ?stack= links now open the FOLDER (feed handler redirects).
    • Settings overhaul + DB-wiring — removed the per-page "Welcome to ShowShak"
      toast; Region persists; creator block from real role; working Curator-handle
      change with live cross-user uniqueness (shared with signup); Payouts =
      coming-soon; Analytics screen removed; Account = real connected logins, no
      change-email, working Deactivate + Delete with 30-day retention (0025).
    • Fixes — views self-collapse so a curator's own re-watches count ONCE
      everywhere (migration 0024); feed audio now CARRIES the unmute across clips
      on scroll (iOS-safe muted-first-then-unmute + canplay re-assert).

  ── EARLIER SESSION (TMDB server-side + player/feed polish; was suite 35) ──
  BIGGEST WIN — TMDB INGEST IS NOW SERVER-SIDE (no more local VPN script):
    • New Edge Function supabase/functions/tmdb-providers/ runs the link →
      providers → genres → write pipeline on Supabase's servers (which reach
      TMDB), so it works hands-off and independent of the frontend host. Modes:
        - batch  (admin secret) → fill all titles missing providers (scheduled).
        - single (titleId)      → refresh one title.
        - search (query)        → proxy TMDB search for the upload box.
        - link   (tmdbId)       → upsert title + cache providers, returns id
                                  INSTANTLY (providers cached in the background
                                  via EdgeRuntime.waitUntil → no 3-4s wait).
        - manual (name+platformIds) → create a title not on TMDB with the
                                  curator-declared platform baked into providers.
    • Auth: batch needs INGEST_SECRET; search/link/manual/single accept a curator
      JWT. Secrets: TMDB_API_KEY + INGEST_SECRET (SUPABASE_* auto-injected).
    • Founder DID: set secrets, deployed (--no-verify-jwt), ran batch --force
      (8 titles cached, 0 failed). ⚠️ MUST REDEPLOY after the search/link/manual
      additions (see §7). Cron schedule via pg_cron + pg_net (SQL editor).

  UPLOAD — LIVE TMDB TITLE SEARCH (replaces the local-DB title search):
    • Curators search the FULL TMDB catalog (proxied via the function — browser
      never touches TMDB). Pick → linked instantly (title + providers stored) →
      clip is platform-filterable from the start.
    • Manual fallback is no longer a dead end: "can't find it" → name + the
      platform(s) it streams on → stored as providers.
    • Curator trim engine (ffmpeg ~25MB) now PRE-WARMS on upload page load for
      signed-in curators (role-gated) → no "preparing editor" wait.

  PLAYER / FEED FELT FIXES:
    • Mute choice now CARRIES across clips (the recycling pool was re-seeding the
      pooled <mux-player>'s stale muted state; now mount+repoint seed from the
      SESSION pref via ssResolveSurfaceMuted).
    • Video object-fit:cover (--media-object-fit) → clips fill full-bleed under
      the floating controls instead of letterboxing into a black band.
    • Watch It now OPENS the platform: ssPlatformWatchUrl → per-platform search
      URL (Netflix/Prime/JioHotstar/etc.) which opens the APP via universal links
      if installed, else the website. (TMDB gives NO true per-title deep link —
      only a JustWatch page — so search-URL is the honest best.)

  PERFORMANCE (page/feed open):
    • Feed-cache key now resolves SYNCHRONOUSLY via a persisted last-user-id
      (ss_last_uid) — was keying to 'guest' before the async session resolved →
      cold fetch every return. Now instant from cache.
    • Cut ~700ms of artificial nav animation (ssNavigate 230→90ms, fade 0.55→0.22s).
    • mux-player loads ASYNC on Discover/Profile/Watchlist AND feed (the surface
      is pre-upgrade-safe: play/pause guarded, muted set via attribute) → posters
      paint instantly, video upgrades in place.
    • Discover + a generic ssReadPageCache/ssWritePageCache → cache-then-revalidate
      so grids paint instantly. Feed cold-open SKELETON (shimmer) when no cache.

  VIEWS SYNC (bug fixed): ssLoadMyClips now selects views_count; removed the
    fabricated fires×11 demo in ssDisplayViews → the card eye count == the owner
    analytics count (both derive from view_events). Migration 0021 APPLIED.

  SHARES (title-blind, real destinations):
    • Clip share → deep link showshak-feed.html?clip=<id> (opens THAT clip via
      ssLoadClipById; feed handles ?clip=); NO show title in the copy.
    • Profile share → showshak-profile.html?curator=<handle> (real public profile).
    • Stack/collection share still pending a destination → see the stack-sharing spec.

  PROFILE: removed "Curating since 2023"; the public profile HIDES the Shared
    Stacks block entirely when the curator has shared nothing (kept, with a prompt,
    on the owner's own profile).

  DISCOVER: platform chips now filter by a clip's REAL availability (platLabel +
    curatorPlat + TMDB provider names, with aliases) + an honest empty state.

  iOS PWA: added apple-touch-startup-image PNGs (data/gen-splash-pngs.js generates
    them) for the launch screen; fixed a standalone bottom-nav black-gap regression
    (the apple meta tags' black-translucent + 100vh; fixed by 100vh→100% feed
    heights + opaque black status bar). CACHE_VERSION now v9.
    ⚠️ The WHITE FLASH on iOS cold launch is STILL NOT fully resolved — deferred
    by founder. (Startup images present but not reliably honored; revisit later.)

  MIGRATIONS: confirmed ALL applied via an object-existence audit; 0016
    (find_or_create_title) was missing → founder applied it. Everything 0001–0021 live.

  NEW SPEC (next active build): .kiro/specs/stack-sharing/ (requirements + design +
    tasks, all validated) — SPEC COMPLETE, NOT yet implemented. See §6.


───────────────────────────────────────────────────────────────
  6b. ACTIVE SPEC — watch-it-curator-availability (SHIPPED ✓)
───────────────────────────────────────────────────────────────
  STATUS: DONE and live. Migration 0028 (content_titles.curator_platform_ids
  uuid[]) is APPLIED by the founder. ssResolveWatchOptions now returns a de-duped
  UNION of TMDB providers + curator-declared platforms (never dead-ends on a
  false "not available"); upload Confirm_Availability_Step + edit-after-post let
  curators declare/correct platforms per title; 11 fast-check properties green.
  The detail below is kept for reference.

  (historical) STATUS: requirements + design + tasks WRITTEN & validated in
  .kiro/specs/watch-it-curator-availability/. NOT yet implemented. This is the
  next thing to build. (The pwa-black-screen-load spec is done through Phase 2;
  its Phase 3 — prerender + bfcache — is deferred, optional polish.)

  THE PROBLEM IT FIXES: a curator linked a TMDB title, published, and Watch It
  said "not available in your region" — but it WAS on Prime; TMDB's provider data
  was just stale/incomplete. Watch It is the product's climax → a false
  "not available" is a trust-breaker.

  PRINCIPLE: TMDB is a HINT, the curator is the AUTHORITY. Watch It must NEVER
  dead-end on "not available" if a curator declared where a title streams.

  THE DESIGN (3 phases, all in design.md):
    - STORAGE: migration 0028 adds content_titles.curator_platform_ids uuid[]
      (per clip↔title; NOT on shared titles.providers, so the TMDB refresh can't
      clobber it and one curator's claim can't change other curators' clips).
    - RESOLVER MERGE: ssResolveWatchOptions changes from "curator-as-fallback-
      only-when-TMDB-empty" to a de-duped UNION of TMDB providers + curator-
      declared platforms (fixes PARTIAL/wrong TMDB, not just empty). "Not
      available" shows only when BOTH are empty. Option shape unchanged →
      ssPlatformWatchUrl + sheet render untouched. In-your-plan still applies.
      Backward-compatible: a clip with no declarations resolves exactly as today.
    - UPLOAD (Confirm_Availability_Step) + EDIT-AFTER-POST: after picking a title
      (TMDB or manual), show resolved region availability + an always-available
      "add/correct platform" chip control (from the platforms catalog), per title;
      stored in curator_platform_ids; editable later.
    - 11 fast-check correctness properties on the pure merge core (union, dedup,
      never-dead-end, shape parity, subscription marking, ordering, region, IN
      default, totality, BACKWARD-COMPAT (critical guard), multi-title).

  EXECUTION: tasks.md has 9 tasks in 3 phases + a wave dependency graph. Phase 1
  (priority, backward-compatible, shippable alone) = task 1 (author migration
  0028) + task 2 (write the 11 property tests, TDD/red-first) + task 3 (implement
  the merge + _ssFetchSheetTitles declaredPlatforms plumbing + _ssPlatformCatalogMap)
  + task 4 (suite green) + task 5 (FOUNDER-RUN: apply 0028). Then Phase 2 (upload
  UI) + Phase 3 (edit). The session was queued to START Phase 1 — resume there.


───────────────────────────────────────────────────────────────
  6. STACKS — SHIPPED (sharing + collaboration + folder view)
───────────────────────────────────────────────────────────────
  STATUS: DONE and live on main. stack-sharing (Phase 1 + 2) and
  stack-folder-view are fully implemented and property-tested (suite 45 green).
  The model below is the SHIPPED reality (not a plan). Specs live in
  .kiro/specs/stack-sharing/ and .kiro/specs/stack-folder-view/. The founder
  applies migrations 0022–0025 in the Supabase SQL editor.

  THE MODEL (private / unlisted / public, + view vs collaborative):
    - Visibility per stack: private (owner only) · unlisted (anyone with the
      link; not listed) · public (listed on the curator's profile).
      Public has a `highlighted` flag → Highlights shelf vs Shared Stacks folder.
    - Roles: curators get all three; normal users get private + unlisted.
    - Shared stacks have a MODE: `view` (watch only) or `collaborative` (members
      co-add clips), capped at SS_STACK_MEMBER_CAP = 6 (configurable), with
      per-clip attribution (added_by) and remove-your-own / owner-removes-any.
    - "Friends & family" = link-sharing (WhatsApp), NOT an in-app relationship.

  KEY DESIGN INVARIANT: unlisted stacks must be NON-ENUMERABLE. Direct table
    SELECT is owner-or-public only; unlisted is reachable ONLY via a SECURITY
    DEFINER RPC get_shared_stack(id) (you must know the UUID). Privacy is
    RLS/RPC-enforced; the pure JS functions are UX-only, never the boundary.

  PHASING (the data model is designed for all of it up front):
    - Phase 1 (shippable alone): visibility columns + RLS + get_shared_stack RPC
      (migration 0022, FOUNDER-RUN) · ssLoadSharedStackById · ssStackShareUrl ·
      ssSetStackVisibility · the 6 pure functions (property-tested) · feed
      ?stack=<id> handler (reuses the clip viewer) · Watchlist visibility/share
      UI · profile public listing + highlight.
    - Phase 2 (additive): stack_members + added_by + collaborative RLS + member
      cap trigger (migration 0023, FOUNDER-RUN) · join-via-link · contribute/
      remove UI · attribution.

  6 CORRECTNESS PROPERTIES (fast-check, see design.md): ssStackCanView,
    ssStackIsListed, ssStackShelfPlacement, ssCanContribute, ssCanJoinStack,
    ssCanRemoveStackItem — assert no view leak, no non-public listing, no cap
    violation, no cross-member removal.

  HOW TO EXECUTE: tasks.md has 12 tasks in 2 phases with a dependency graph +
    wave plan. Start Task 1 (pure functions + property tests) and Task 2 (write
    migration 0022 for the founder to apply). Keep the suite green at every step.

  NOTE — the clip-player-performance build (the PRIOR active spec) is DONE
    (Phases 1-5 shipped). Only Phase 6 (founder real-device Mux Data measurement)
    + the deferred Phase 7 (360p MP4) remain — see §7. Locked player decisions
    still hold: no MP4 yet, no CDN swap, no player swap, no unbounded memory cache.


───────────────────────────────────────────────────────────────
  7. FOUNDER-RUN ITEMS PENDING (only YOU can do these)
───────────────────────────────────────────────────────────────
  CONFIRMED DONE: tmdb-providers function deployed + secrets set + batch ran;
  migrations 0001–0028 applied; AND this session: migration 0029 (DMCA Phase 1)
  APPLIED ✓, migration 0031 (beta-consent-gate) APPLIED ✓, Anonymous sign-ins
  ENABLED ✓ in Supabase Auth. Code pushed to main (commit 99e004a, CACHE_VERSION v30).
  (0030 is RESERVED for DMCA Phase 2 — not created yet.)

  STILL PENDING — ⚠️ TO FULLY LIGHT UP THE BETA COMPLIANCE SURFACES:
  - RUN supabase/seed/seed_policy_versions.sql in the Supabase SQL editor. It is
    READY TO RUN (real ToS/Privacy/Curator-Terms bodies inlined verbatim, dollar-
    quoted as $SS$). Idempotent. Until it runs, showshak-legal.html shows the
    placeholder scaffolding (graceful fallback), and the consent/curator gates
    fail-soft ("policies unavailable") because no current policy_versions rows exist.
  - DEPLOY the DMCA intake Edge Function: supabase functions deploy submit-takedown
    --no-verify-jwt  (public anon takedown intake — code is pushed, not yet deployed).
  - FILL the [PLACEHOLDERS] in legal/*.md (entity name/type, registered address,
    [CITY] for jurisdiction, grievance/copyright/support emails, Grievance Officer
    name, effective date, version, website URL). Use REAL monitored inboxes for the
    grievance + copyright emails from day one (an unreachable Grievance Officer is
    the classic IT-Rules failure). Then book ONE IP/tech-lawyer hour to bless the
    filled-in docs. Re-seed a NEW version row (recipe in the seed file footer) so the
    "counsel review required" banner clears. DON'T go wide to real users until this is
    done. (Decide the operating ENTITY first — company vs individual.)
  - After any of the above that needs on-device, reopen the installed PWA twice (v30).

  STILL PENDING (older, not blocking):
  - SET UP the 30-day account-purge cron (service role) for 0025 — skeleton is in
    the migration's footer. Until then, deleted accounts stay hidden + recoverable.
  - REDEPLOY tmdb-providers ONLY if its live upload TMDB search ever stops working:
    supabase functions deploy tmdb-providers --no-verify-jwt
  - iOS WHITE FLASH on cold launch — still deferred (re-check on device).
  - PHASE 6 (player build): a real mid-range Android / 4G Mux Data session vs
    targets (TTFF <800ms wifi / <1.5s 4G, scroll-to-play ~150ms, rebuffer <1%).


───────────────────────────────────────────────────────────────
  8. OPEN / DEFERRED (not blocking)
───────────────────────────────────────────────────────────────
  - ✓ ANDROID "NaN" / "undefined" on the feed action rail + Watch It label —
    FIXED in v21 (see SESSION LOG). fmtFires hardened (non-finite → 0) + defaulted
    fires/platAbbr/platColor/platLabel/platRgb in _inlineClipHTML + _inlineSyncRail;
    CACHE_VERSION bump cleared the stale Android SW bundle. No longer open.
  - FEED prefetch / SW Segment_Cache are OFF by default (opt-in via localStorage
    ss_ff_segcache='on'). If/when we want true scroll-back persistence, VALIDATE
    the SW 206 range path on-device first (DevTools Network + Cache Storage),
    then consider flipping default-on. The pure ssSegmentEvictionPlan is tested;
    the impure SW path is not device-verified yet.
  - Update the `overview` brief to record the "views on clips"
    decision (it currently says "no view counts on clips" — that
    rule was deliberately changed this session).
  - A rare FLAKY property test (~1 in ~10 runs) lives in a test file
    we did NOT touch this session (pure-helpers passes every run);
    worth a focused hunt: run each prop-*.test.js in isolation ~200x
    to find + pin the seed. Not from our changes.
  - RETENTION / TOKEN reward idea (free OTT subscription for tokens):
    POST-traction only (after ~15 curators + ~100 returning users).
    Reframe: it's an OTT-FUNDED trial-acquisition engine (they pay,
    not us), gated on one promo-code partnership, with anti-fraud +
    data-hygiene built in. Do NOT self-fund free subs. For NOW,
    retention = intrinsic ("new clips from curators you follow").
  - The big company milestones (per pitch file): 3-sided cold-start
    (15 curators + 100 returning users), LEGAL/DMCA safe-harbor
    scaffolding (the #1 existential risk), Watch-It paid attribution,
    then the PWA layer.


───────────────────────────────────────────────────────────────
  9. HOW WE WORK (the operating contract)
───────────────────────────────────────────────────────────────
  - Big/central/architectural work → SPEC IT (requirements → design →
    tasks), guarded by property tests, phased so nothing breaks.
  - Small, contained bugs/changes → just FIX directly, verify, done.
    (Don't over-ceremony quick fixes.)
  - Always run `node tests/run-all.js` after shared.js changes.
  - Commit in clean logical groups; push to main only when asked
    (founder checks the live page to confirm).
  - The AI is a COFOUNDER: pushes back with honest insight, does the
    cost/market math, protects the product's soul, and never just
    agrees. Corrects the founder when wrong — respectfully.


═══════════════════════════════════════════════════════════════
  >>> KICKOFF PROMPT — COPY EVERYTHING BELOW INTO THE NEW CHAT <<<
═══════════════════════════════════════════════════════════════

You are my COFOUNDER on ShowShak, and you are operating like our lives
depend on this — because they do. This is our one shot to make ShowShak
the next big tech, and I need you all-in on it with me, not as an
assistant but as the partner who owns this with me.

WHO YOU ARE ON THIS TEAM (bring all of it to every reply):
  • A SEASONED STARTUP OPERATOR — you have built and scaled real products,
    so you think in wedges, three-sided cold-start, retention loops, and
    sequencing. You know what actually moves the needle pre-traction vs
    what's a distraction, you do the cost/market math out loud, and you
    protect focus and runway. You tell me when something is premature.
  • A SHARP MARKET & CUSTOMER STRATEGIST — you carry real insight about the
    streaming-discovery space and our launch market (India): the
    fragmentation pain, curators-not-influencers, the monetization ladder
    (promoted clips → UA/CPA → cross-platform data), the neutrality moat,
    and the legal/DMCA existential risk. You know what investors will probe
    and what real users actually feel, and you frame decisions that way.
  • A GREAT ENGINEER — deep, hands-on technical execution. You write clean,
    correct, property-tested code, you respect the existing architecture and
    the sacred product rules, you read the code before you change it, and
    you ship working software — verified, not sketches.
  • AN HONEST PARTNER — no stale, generic, or yes-man answers. Push back with
    real reasoning, correct me when I'm wrong (respectfully), protect the
    product's soul, and make every credit and every second return huge value.

HOW YOU DEBUG (learned rule): when a bug could have more than one cause,
don't stop at the first thing you fix — check the other candidates too,
confirm the real root cause, and fix it properly. Verify with the test
suite + diagnostics before claiming done.

ShowShak in one line: a trust-based, creator-curated, action-oriented
STREAMING DISCOVERY layer — curators post short vertical clips
recommending shows/movies; users trust the curator and tap "Watch It"
to go to the streaming platform. It is NOT a streaming service and NOT
social media. We are building the website first as the reference for a
future native app. Launch market: India.

Before anything, READ these files in the repo to load full context and
confirm you understand the project, the sacred philosophy, and exactly
where we are:
  - COFOUNDER-HANDOFF.txt  (START HERE — this is the full brief +
    current state + next steps)
  - overview
  - backend-architecture.md and backend-schema.md
  - points to keep in mind pitch wise.txt
  - showshak-shared.js  (the heart of the app)
  - .kiro/specs/ — stack-sharing + stack-folder-view (both SHIPPED; read as
    reference for the patterns + where new specs go)

Sacred rules you must never break: Creator-first/title hidden until
Watch It; Fire = the like; Watch It must feel frictionless & private;
HIDE THE SCOREBOARD (public shows only content signals — per-clip
fires + views, creator, followers/clips — never fires-received totals
or Watch-It taps; enforced by RLS). Tech is pure vanilla HTML/CSS/JS
(no build step), pure logic in showshak-shared.js dual-exported for
Node + fast-check tests (`node tests/run-all.js`); backend Supabase +
Mux (pay-as-you-go) + TMDB; migrations applied manually by me in
Supabase; hosting GitHub Pages on main.

How we work: big/architectural work gets a proper spec
(requirements → design → tasks) guarded by property tests and phased
so the Feed never breaks; small contained fixes you just do directly
and verify. Always run the test suite after touching shared.js. Only
commit/push to main when I ask.

MY SETUP (important): I have the project's LIVE PAGE via GitHub Pages on the
`main` branch — every push auto-rebuilds it and I watch all changes there. I also
have the PWA INSTALLED on my phone, so I test on a real device. Because the PWA
caches via the service worker, BUMP CACHE_VERSION in sw.js whenever you push code
I need to see (it's currently v14); I close+reopen the app (or remove → re-add) to
pick up the new version. I also run the SQL migrations myself in the Supabase SQL
editor (you write them, I apply them) and do the on-device verification checks.

CURRENT FOCUS / WHERE WE ARE: We are building DMCA / MODERATION SCAFFOLDING and
preparing an OPEN BETA in India. The plan: prove the loop (~15-20 curators, 300-500
clips, ~1000-1500 users), then raise on that traction and buy a real legal team.
The site is PUBLIC; we control risk by limiting the curator (upload) side, NOT by a
"private/testing" label. Music feature stays PARKED. 18+ only.

LIVE ON MAIN (CACHE_VERSION v31 = feed-follows, commit 05afc86; v32 = felt-quality follow-ups
[clip brightness + instant profile stats], PUSH-PENDING — confirm it's on main; migrations
0001-0031 applied — 0030 is RESERVED for DMCA Phase 2, so consent uses 0031): the felt-quality +
reliability sweep, watch-it-curator-availability, feed-clip-load-performance, server-side Mux trim,
portrait lock, analytics/highlights/discover polish, DMCA Phase 1 + beta-consent-gate, PLUS this
session's FEED-FOLLOWS ranker (below). Pure logic in showshak-shared.js, dual-exported +
property-tested (node tests/run-all.js → now 89 files green).

SHIPPED THIS SESSION (founder-side ops noted in §7):
  - feed-follows, spec .kiro/specs/feed-follows/ — THE COLD-START LEVER: the flat
    newest-first feed is now a TIERED, TRUST-WEIGHTED feed: 1) new clips from curators
    you follow → 2) other new clips → 3) followed curators' older popular clips →
    4) global popular → 5) randomized rest. Pure ssRankFeed / ssSliceRankedPage /
    ssFeedTier (Recency_Window 14d) / ssPopularityScore (fires*3 + views*1) + a seeded
    PRNG (deterministic Tier 5 + stable pagination), 13 fast-check properties. Wired
    behind the UNCHANGED ssLoadClips: candidate fetch → rank (cached once per feed
    session) → slice → hydrate via the existing rich select. HIDE THE SCOREBOARD enforced
    at the data layer (candidate query selects ONLY id/creator_id/created_at/fires_count/
    views_count — never a private metric). Seen clips sink within their tier; empty-follow/
    guest degrades to ~today's feed. ss_ff_ranker='off' kill switch + automatic flat-feed
    fallback on ANY error. Seen-state in localStorage 'ss_seen_v1_<uid>' next to ssRecordView
    — NO migration. 3 unit-feed-* shell tests. The TDD tests caught a BLANK-FEED bug (ranker
    rejected the status-less candidate rows) before it shipped. SHIPPED to main, commit
    05afc86, CACHE_VERSION v31. Frontend-only.
  - felt-quality follow-ups (on-device feedback) → CACHE_VERSION v32 (PUSH-PENDING — confirm
    on main): (1) clip vignette lightened (inline .clip-vignette + fullscreen .ssv-vig) so the
    clip reads as the FOREGROUND, not a dimmed backdrop — bottom legibility scrim kept, top
    tint removed; (2) profile follower/clip-count stats prefetched into the ssPrewarmProfile
    cache so the creds strip paints REAL numbers INSTANTLY on landing (merge-safe cache,
    revalidated by fetchOwnFollowers + hydrateMyClipsFromDB).
  - PREVIOUSLY SHIPPED (now LIVE on main — full detail in SESSION LOG §5): DMCA Phase 1
    (migration 0029 APPLIED) + beta-consent-gate (migration 0031 APPLIED + Anonymous sign-ins
    ENABLED). The founder-side beta-compliance ops are STILL PENDING — see NEXT STEP + §7.

DMCA PHASE 2 (workflow/admin/counter-notice/repeat-infringer; tasks 11-18) is
DEFERRED to post-raise — at 500 clips a takedown is actioned by flipping
content.status='removed' in Supabase (RLS hides it everywhere).

NEXT STEP (resume here):
  FOUNDER-SIDE (only YOU can do — see §7; STILL PENDING from the DMCA/consent work, these
  gate going WIDE to real users): (a) RUN supabase/seed/seed_policy_versions.sql in the
  Supabase SQL editor so the beta shows the real policy text; (b) DEPLOY the DMCA intake fn:
  supabase functions deploy submit-takedown --no-verify-jwt; (c) fill the [PLACEHOLDERS] in
  legal/*.md (entity/emails/Grievance Officer) + book the IP-lawyer hour, then re-seed a new
  version row to clear the counsel banner. Open question: the operating ENTITY (company vs
  individual).
  CONFIRM: push v32 (clip brightness + instant profile stats) to main if not already, and
  reopen the installed PWA twice to pick it up.
  BUILD (feed-follows is SHIPPED — watch it on-device with real follows). The feed only rewards
  trust once users actually follow curators, so the next levers are about FILLING + SURFACING
  the follow graph: (i) curator discovery / suggested-curators so users build follows;
  (ii) a "new from curators you follow" surfacing/notification; (iii) tune the ranker
  (Recency_Window 14d, popularity 3:1 weight, seen-state) once real beta data exists. Spec the
  big ones (requirements → design → tasks), phase so the Feed never breaks.
  PARKED (post-traction): MUSIC — royalty-free only (Pixabay/Jamendo), NEVER
  Spotify/Apple, NEVER a copyrighted "use this song" library (inducement).

Do NOT relitigate locked player decisions (no MP4, no CDN swap, no player swap,
no unbounded memory cache; ONE behaviour for iOS+Android, recycled player pool).

Start by reading the files above, give me a tight confirmation that you've got the
full picture and current state, then continue from NEXT STEP. Bring your A-game.

═══════════════════════════════════════════════════════════════
  END OF KICKOFF PROMPT
═══════════════════════════════════════════════════════════════
